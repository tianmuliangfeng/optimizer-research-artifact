# External repositories

Third-party repositories and datasets are intentionally not vendored. Place a
pinned Newton-Muon checkout at `third_party/Newton-Muon-official-r0/` or set
`SNM_OFFICIAL_REPO` to an external location.

The `licenses/` directory is intentionally tracked and preserves notices for
third-party material referenced or adapted by this release.
